[pytest]
pythonpath = .
testpaths = tests
# The pytest cache folder does not work well inside OneDrive, so we turn it off.
addopts = -p no:cacheprovider
# Hide warnings that come from inside the libraries, not from our code.
filterwarnings =
    ignore::DeprecationWarning
    ignore:.*httpx.*
